# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# WASM build output
/pkg
/web/pkg

# Deploy stamp (written by scripts/write-health.mjs / CI)
/web/health.json

# Node (frontend toolchain, when added)
node_modules
dist
.next
.svelte-kit

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

.ublx.toml
.cursor
